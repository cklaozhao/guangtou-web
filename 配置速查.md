# 配置速查

`guangtou.html` 里所有"想动一下数字就能调"的参数集中清单。行号基于 `logo-4` 分支当前快照。

> 用法：表里某项想改时，直接打开 `guangtou.html` 跳到对应行号改一个数字，浏览器双击文件验证视觉，commit + push。

## 1. 扩散 / 过渡速度

| 项 | 当前值 | 行号 | 说明 |
|---|---|---|---|
| **头像进入 / 离开过渡时长** | **800 ms** | `guangtou.html:696` | 一个常量 `transitionMs` 同时管进入和离开（对称） |
| 缓动曲线 | `easeOutCubic` ≡ `1 - (1-t)³` | `guangtou.html:726` | 开头较快、结尾沉淀。改成 `easeInOutCubic` 会两端都缓 |
| cursor-ring 自身缩放过渡 | 0.2 s | `guangtou.html:299-300` | 圆圈大小切换（200 ↔ 100 ↔ 0/隐藏）的 CSS transition |
| cursor-ring opacity 过渡 | 0.15 s | `guangtou.html:301` | 鼠标进出 hero 区时的淡入淡出 |

## 2. 圆圈直径（cursor-ring 视觉尺寸）

| 状态 | 直径 | 行号 | 备注 |
|---|---|---|---|
| **(a) 默认 / hero 空白** | **200 px** | `guangtou.html:278-279` | `.cursor-ring { width/height }` |
| **(b) 在文字上**（标题 / footer 链接） | **100 px** | `guangtou.html:305-306` | `.cursor-ring.is-hidden { width/height }` |
| **(c) 在头像上** | **100 px**（线条小窗）| `guangtou.html:695` | cursor-ring 自身被 `visibility: hidden` 隐藏（line 311-317）；视觉上你看到的是 mask 揭示的"线条小窗"，由 `innerWindowR = 50`（半径）控制，直径 = 2 × 50 = 100 px |

> ⚠️ **半径 vs 直径**：CSS `width/height` 是**直径**，但 mask 参数（`innerWindowR` / `cursorRingRadius`）是**半径**。视觉对齐时记得 `直径 = 2 × 半径`。

## 3. 其他可配置参数

### 3.1 mask 半径（影响头像上的视觉）

| 项 | 当前值 | 行号 | 备注 |
|---|---|---|---|
| hero 空白时跟随圆**半径** | **100**（视觉直径 200 px，与 cursor-ring 重合） | `guangtou.html:694` `cursorRingRadius` | 改这个的同时通常要改 §2 (a) 的 cursor-ring `width/height` 为 2 倍，否则出现"双圈" |
| 头像稳定态**线条小窗半径** | **50**（视觉直径 100 px） | `guangtou.html:695` `innerWindowR` | 头像上 cursor 处那个露线条的小圆。当前已与字上 cursor-ring 直径对齐 |
| 头像本身视觉尺寸 | `clamp(150px, 18vw, 190px)` | `guangtou.html:139-140` | 头像直径，响应式 |

### 3.2 头像 3D 互动

| 项 | 当前值 | 行号 | 备注 |
|---|---|---|---|
| hover 放大倍数 | **1.06**（即 +6%） | `guangtou.html:799` | `scale(1.06)`。改成 1.0 = 不放大 |
| 3D 倾斜最大角度 | **12 度** | `guangtou.html:799` | `* 12deg`。鼠标在头像边缘时 X / Y 旋转幅度。改 0 = 不倾斜，改 20 = 更夸张 |

### 3.3 鼠标跟随平滑度

| 项 | 当前值 | 行号 | 备注 |
|---|---|---|---|
| cursor-ring 跟随 lerp 因子 | **0.18** | `guangtou.html:651` | 每帧朝鼠标插值 18%。改大更跟手（1 = 完全贴鼠标），改小更软（0.05 = 明显拖尾） |

### 3.4 响应式

| 项 | 当前值 | 行号 | 备注 |
|---|---|---|---|
| cursor-ring 移动端隐藏断点 | 屏宽 ≤ 640 px | `guangtou.html:381, 396-398` | 移动端没鼠标，所以 `display: none` |

---

## 速查总表（一行一参数）

| # | 参数 | 当前值 | 文件:行号 |
|---|---|---|---|
| 1 | 头像进入 / 离开过渡时长 | 800 ms | guangtou.html:696 |
| 2 | 缓动曲线 | easeOutCubic | guangtou.html:726 |
| 3 | cursor-ring 缩放过渡 | 0.2 s | guangtou.html:299-300 |
| 4 | cursor-ring opacity 过渡 | 0.15 s | guangtou.html:301 |
| 5 | 默认 cursor-ring 直径 | 200 px | guangtou.html:278-279 |
| 6 | 字上 cursor-ring 直径 | 100 px | guangtou.html:305-306 |
| 7 | 头像上 cursor-ring | visibility: hidden | guangtou.html:311-317 |
| 8 | hero 跟随圆半径 | 100 | guangtou.html:694 |
| 9 | 头像线条小窗半径 | 50 (= 直径 100) | guangtou.html:695 |
| 10 | 头像本身尺寸 | clamp(150, 18vw, 190) | guangtou.html:139-140 |
| 11 | hover 放大倍数 | 1.06 | guangtou.html:799 |
| 12 | 3D 倾斜最大角 | 12 deg | guangtou.html:799 |
| 13 | cursor 跟随 lerp | 0.18 | guangtou.html:651 |
| 14 | 移动端断点 | 640 px | guangtou.html:381 |

> 改完任何一项，记得 commit + push 一次（项目约定：每次落定都推 origin）。
